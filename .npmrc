# Peer dependencies — relaxed to allow Storybook and testing tools
# that declare wider peer dep ranges
strict-peer-dependencies=false

# Hoist packages needed by Next.js and Storybook
shamefully-hoist=true
